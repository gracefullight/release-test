name: Main

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build
        run: echo ${{ github.sha }} > Release.hash
      - name: Test
        run: cat Release.hash
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            Release.hash
          prerelease: ${{ contains(github.ref, '-beta.') }}

